node_modules/
dist/
*.tsbuildinfo
coverage/
artifacts/

# 工具/plugin 产物（不属于 OpenArch）
.reasonix/

# 协调服务本地投影缓存（disposable，可从 Git 重建；默认在系统临时目录，显式 --projection 覆盖时可能落在工作区）
coordination-projection.ndjson

# .openarch/ 边界（design §3.3）：config + baseline + history 受 Git 版本控制。
# 仅排除以下本地文件：
.openarch/.docs-repo-config.json
.openarch/docs-repo
.openarch/.*.lock
.openarch/scan-status.json
.openarch/pending/
.openarch/baseline.staging-*/

# 内部文档（release 分支发布时不包含；main 开发分支保留）
docs/internal/
.obsidian/
# Agent 开发运行时产物（release 发布不包含；main 开发分支保留）
.claude/
.agents/
# Agent 开发指南（release 发布不包含；main 开发分支保留）
AGENTS.md
CLAUDE.md
# 运行时校准样本（seed-symbol-calibration.mts 可重建；release 不包含）
.openarch/calibration/
simulation.md
